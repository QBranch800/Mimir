# Only what Mimir imports directly. pip works out the rest, which matters because
# some of those differ per operating system: a full `pip freeze` taken on a Mac
# pins pyobjc and macholib, and installing that on Windows fails outright.

# fetching and filtering
requests==2.34.2
python-dotenv==1.2.3
trafilatura==2.2.0

# scoring
google-genai==2.24.0
pydantic==2.13.5

# reading the briefing
Flask==3.1.3

# the desktop app: a native window, and the tool that packages it
pywebview==6.2.1
pyinstaller==6.22.3
